.. index::
   single: command-line interface
   single: CLI
   single: object-verb grammar

Command-Line Reference
======================

The ``ftmwpipeline`` command-line interface: the object-verb grammar, the
per-stage ``run``/``show`` commands, and the cross-cutting ``settings`` and
``scan`` meta-objects.

.. note::

   This page is in preparation.
