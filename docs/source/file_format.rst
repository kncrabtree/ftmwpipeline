.. index::
   single: .ftmw file
   single: file format
   single: HDF5
   single: provenance
   single: stage tracking

The .ftmw File
==============

The self-contained, portable HDF5 file that carries one experiment through the
pipeline: the per-stage results, the dependency tracking between stages, the
settings each stage used, and the source provenance.

.. note::

   This page is in preparation.
