.. index::
   single: peak fitting
   single: nonlinear least squares
   single: line shape
   single: leakage-wing baseline
   single: residual rescue

Stage 5: Peak Fitting
=====================

Fitting the peaks in each window to a finite-duration line-shape model: the
per-window nonlinear least-squares fit, the decay-time penalty, the
leakage-wing baseline, residual rescue, spur masking, and the statistical
acceptance of each fitted line.

.. note::

   This page is in preparation.
