"""
Bundled Stage 5 fit presets.

A preset is a YAML file describing a partial
:class:`~ftmwpipeline.core.stage_fit_settings.StageFitSettings`. The file
ships either as a packaged resource here (referenced by bare name) or as
a user-authored YAML on disk (referenced by path). See
:func:`ftmwpipeline.core.stage_fit_settings.load_preset` for the
resolution rules.
"""
